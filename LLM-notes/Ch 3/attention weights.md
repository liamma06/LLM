*What is the difference between attention weights and weight parameters?* 

Weight parameters are the values that would be actually optimized and changed during the training of the model. While attention weights affect the context vector(a vector that shows how the other parts of the input token effect the chosen token.)
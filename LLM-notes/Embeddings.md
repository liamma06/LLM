*mapping from discrete objects to a continuous vector space*

for embeddings used by an LLM we would need to split text into words, convert words into tokens, and turning tokens into embedding vectors which is the process of [[tokenizing text]] 